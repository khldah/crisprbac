services:
  - type: web
    name: my-flask-app
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt --verbose
    startCommand: gunicorn app:app